<?php
  $n=273;
//$n=271; //просто число
  if ($n<2 || intval($n) != $n) { // n е по-малко от 2 или не е цяло => не може да е просто
    $yes='not';
  }
  else {  //проверка има ли поне един делител по-малък от n
    $yes='';
    $i=2;
    while ($i < $n) {
      if ($n % $i == 0) { // i е делител => n не е просто
        $yes='not';
        echo "divided on $i<br/>";
        $i=$n;
      }
      $i++;
    }
  }
  echo "$n - is $yes simple";
?>
